/**
 * state
 *
 * @export
 * @interface State
 */
export interface State {
    /**
     * test property
     *
     * @type {boolean}
     * @memberof State
     */
    test: boolean;
}

const state: State = {
    test: true,
};

export default state;
